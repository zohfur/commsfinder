# Build instructions:

Extension is built with npm v11.5.1 and node v24.4.1 but any node version >21 should work fine.
Run ``npm i`` to install dependencies.
Run ``npm run build:firefox`` to build the extension with Webpack.
Built extension can be found in dist/firefox/commsfinder-{version}.zip.
